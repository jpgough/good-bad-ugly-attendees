spring:
  cloud:
    gateway:
      default-filters:
      routes:
        - id: test
          uri: http://192.168.1.5:12345/
          predicates:
            - Path=/a/**
          filters:
            - RewritePath=/a/(?<segment>.*), /$\{segment}/
